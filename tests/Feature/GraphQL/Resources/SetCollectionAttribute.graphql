mutation SetCollectionAttribute(
    $collectionId: BigInt!
    $key: String!
    $value: String!
) {
    SetCollectionAttribute(
        collectionId: $collectionId
        key: $key
        value: $value
    ) {
        id
        encodedData
    }
}
